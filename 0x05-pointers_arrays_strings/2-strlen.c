#include <string.h>
#include "main.h"

/**
 * _strlen - function that returns the length of a string.
 *
 * @s: input parameter.
 *
 * Return: Always 0.
 */

int _strlen(char *s)
{
	size_t _strlen(char *s);
}
